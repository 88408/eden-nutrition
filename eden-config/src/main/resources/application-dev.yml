# ============================================
# 开发环境配置
# ============================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/eden_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: eden
    password: edenpass

  redis:
    host: localhost
    port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password

elasticsearch:
  host: localhost
  port: 9200

logging:
  level:
    eden: DEBUG
    org.mybatis: DEBUG

swagger:
  enabled: true
