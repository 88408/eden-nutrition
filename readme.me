#eden-nutrition
一、项目名称
伊甸滋补线上商店（Eden Nutrition E-Commerce Platform）

二、项目简介
一个专注于高端滋补品（如燕窝、人参、阿胶、灵芝等）的 B2C 电商平台，支持商品浏览、购物车、订单管理、用户评价、优惠券、秒杀活动、会员积分等功能。采用 SSM 框架，遵循企业级开发规范，具备高内聚、低耦合、可测试、可部署特性。

三、技术栈（大厂简历级重点）
类别
技术选型
后端框架
Spring 5.3 + Spring MVC + MyBatis 3.5
数据库
MySQL 8.0（主从读写分离可选）
缓存
Redis（Session 共享、热点商品缓存、分布式锁）
消息队列
RabbitMQ（订单异步处理、邮件通知）
安全
Spring Security + JWT（或自定义 Token）
搜索
Elasticsearch（商品全文检索）
文件存储
阿里云 OSS / 本地 MinIO
前端（可选）
Vue3 + Element Plus（或 Thymeleaf 渲染）
构建工具
Maven
部署
Docker + Nginx + Linux
监控
Prometheus + Grafana（可选）
日志
Logback + ELK（可选）
API 文档
Swagger3（OpenAPI 3.0）
📌 简历亮点：强调自己实现了 分布式锁处理超卖、Redis 缓存穿透/雪崩防护、MyBatis 二级缓存优化、RabbitMQ 延迟队列实现订单超时取消 等企业级问题。

四、核心功能模块
1. 用户模块
注册/登录（手机号 + 验证码 / 用户名密码）
JWT Token 认证 + 拦截器权限控制
用户信息管理（收货地址、头像、积分）
2. 商品模块
商品分类（一级/二级分类）
商品详情（图文、规格参数）
商品搜索（Elasticsearch 实现关键词、价格区间、销量排序）
商品评价（带图评价、星级评分）
3. 购物车模块
添加/删除/修改商品数量
Redis 存储购物车（支持未登录用户临时购物车）
4. 订单模块
创建订单（扣减库存、生成订单号）
订单状态管理（待支付、已支付、已发货、已完成、已取消）
超时未支付自动取消（RabbitMQ 延迟队列）
5. 支付模块（模拟）
支付宝/微信沙箱支付对接（可模拟）
支付回调处理（幂等性校验）
6. 促销模块
优惠券系统（满减、折扣券、领取/使用）
秒杀活动（Redis 预减库存 + RabbitMQ 异步下单 + 分布式锁防超卖）
7. 后台管理（Admin）
商品管理（CRUD、上下架）
订单管理
用户管理
数据统计（日活、销售额、热销商品）
五、关键设计亮点（简历中重点描述）
✅ 1. 高并发秒杀设计
前端：按钮置灰、倒计时
后端：
Redis 预减库存（DECR 原子操作）
请求入 RabbitMQ 异步下单
消费者使用 Redis 分布式锁（Redisson）防止超卖
库存回滚机制（订单失败时）
✅ 2. 缓存策略优化
商品详情页：Redis 缓存 + 空值缓存（防穿透）
缓存雪崩：设置随机过期时间
缓存击穿：热点商品永不过期 + 后台定时更新
✅ 3. 分布式事务（可选进阶）
使用 本地消息表 + 定时任务补偿 保证“扣库存”与“创建订单”一致性
或使用 RabbitMQ Confirm + 手动 ACK 保证消息可靠投递
✅ 4. 安全防护
SQL 注入：MyBatis 参数绑定
XSS：前端转义 + 后端过滤
接口限流：Guava RateLimiter / Redis + Lua 脚本
敏感操作日志审计
六、数据库设计（核心表）
sql
1234567891011121314151617181920212223242526
-- 用户表
user(id, username, phone, password, avatar, create_time)

-- 商品分类
category(id, name, parent_id, level)

-- 商品表
product(id, name, category_id, price, stock, detail, status, sales)

-- 购物车（Redis 存储，可不建表）

七、项目结构（Maven）
12345678
eden-nutrition/
├── eden-common        // 工具类、常量、异常
├── eden-pojo          // 实体类
├── eden-mapper        // MyBatis Mapper 接口 + XML
├── eden-service       // 业务逻辑
├── eden-web           // Spring MVC Controller + 拦截器
├── eden-admin         // 后台管理模块（可合并到 web）
└── eden-config        // Swagger、Redis、RabbitMQ 配置
八、部署与测试
单元测试：JUnit + Mockito（Service 层覆盖率 > 80%）
接口测试：Postman + Swagger
压测：JMeter 测试秒杀接口（QPS > 1000）
Dockerfile：一键部署 MySQL + Redis + App
九、简历描述建议（STAR 法则）
项目名称：伊甸滋补线上商店（SSM 电商系统）
项目描述：基于 Spring + Spring MVC + MyBatis 构建的高性能滋补品电商平台，支持高并发秒杀、分布式缓存、异步订单处理等企业级特性。
技术亮点：

使用 Redis + RabbitMQ 实现秒杀系统，通过预减库存与异步下单将 QPS 提升至 1200+，并通过 Redisson 分布式锁解决超卖问题；
设计多级缓存策略（本地缓存 + Redis）降低数据库压力，缓存命中率达 95%；
基于 JWT + 拦截器实现 RBAC 权限控制，支持用户与管理员角色分离；
使用 RabbitMQ 延迟队列实现订单 30 分钟自动取消，保障库存及时释放。
成果：系统稳定运行，支持日均 1 万+ 访问量，代码规范通过 SonarQube 扫描。