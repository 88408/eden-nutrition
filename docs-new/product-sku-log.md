[2026-06-09 23:55:00] 满分迭代：商品 SKU 规格能力
- 修改内容：新增 `product_sku` 表、商品 SKU 实体、Mapper、Service 和后台 `/admin/product/{id}/skus` 查询/保存接口；用户端商品详情返回并展示 `skuList`；订单明细新增 `sku_id` 和 `sku_spec_name` 快照字段，SKU 下单路径支持规格库存扣减和订单快照保存。
- 注意事项：旧购物车下单逻辑保持兼容；当前购物车仍按主商品加购，SKU 选择主要用于详情展示和后端直购接口能力。
[2026-06-10 13:28:58] SKU 购物车下单闭环修复
- 修改内容：购物车项增加 skuId 和 skuSpecName，Redis 购物车字段改为兼容 productId 与 productId:skuId；用户端商品详情加入购物车/立即购买会传入所选 SKU，购物车和结算页展示规格快照；订单从购物车下单时复用 SKU 扣库存与订单快照逻辑。
- 注意事项：商品存在启用 SKU 时必须选择规格才能加入购物车或购买；旧购物车中不带 SKU 的普通商品项仍按原逻辑兼容。
[2026-06-10 13:39:56] SKU 必选校验和禁用规格修复
- 修改内容：后端购物车和下单链路补充 SKU 强校验，商品存在启用 SKU 时不允许空 skuId 下单，传入 skuId 时必须校验规格存在、属于当前商品且状态启用；用户端商品详情将禁用或无库存 SKU 置灰并禁止选择。
- 注意事项：无启用 SKU 的普通商品仍允许按原商品维度加入购物车和下单；后续若支持预售规格，需要重新定义 SKU 状态和库存校验规则。
[2026-06-10 13:51:35] SKU 直购无效规格和无库存默认选择修复
- 修改内容：后端 SKU 下单补充无效 skuId 校验，传入不存在的 SKU 会直接拒绝；用户端商品详情默认只选中启用且有库存的 SKU，并在加入购物车或立即购买前统一校验可购买规格。
- 注意事项：无库存 SKU 继续展示为不可用，不支持超卖或预售；商品无启用 SKU 时仍保持普通商品下单兼容。
