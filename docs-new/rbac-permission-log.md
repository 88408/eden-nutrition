[2026-06-09 23:55:00] 满分迭代：后台 RBAC 细粒度权限
- 修改内容：新增权限、角色、角色权限、用户角色后端模型、Mapper、Service 与后台接口；新增 `@RequirePermission` 权限注解并升级后台拦截器，使接口可按权限码校验；管理员登录和信息接口返回角色、权限码和菜单树；管理端侧边栏按权限码动态显示菜单，并新增角色权限页面。
- 注意事项：需执行 `sql/10-full-score-iteration.sql` 初始化权限、角色和管理员角色关系；超级管理员默认拥有全部权限，便于演示。
[2026-06-10 13:28:58] 角色权限回显与防误清空修复
- 修改内容：管理端角色权限页新增角色已有权限加载逻辑，选择角色时调用后端角色权限接口回显已分配权限；保存后重新加载当前角色权限，避免刚选中角色时空数组保存导致权限被误清空。
- 注意事项：演示 RBAC 时应先确认当前管理员拥有 rbac:manage 权限；接口失败时页面会保留原状态并提示加载失败。
