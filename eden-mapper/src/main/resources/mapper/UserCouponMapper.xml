<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eden.mapper.UserCouponMapper">

    <resultMap id="BaseResultMap" type="eden.pojo.UserCoupon">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="coupon_id" property="couponId"/>
        <result column="order_id" property="orderId"/>
        <result column="status" property="status"/>
        <result column="used_time" property="usedTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, coupon_id, order_id, status, used_time, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectAvailableByUserId" resultMap="BaseResultMap">
        SELECT uc.id, uc.user_id, uc.coupon_id, uc.order_id, uc.status, uc.used_time, uc.create_time
        FROM user_coupon uc
        INNER JOIN coupon c ON uc.coupon_id = c.id
        WHERE uc.user_id = #{userId} AND uc.status = 0 AND c.end_time > NOW()
        ORDER BY c.end_time ASC
    </select>

    <select id="selectByUserAndCoupon" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        WHERE user_id = #{userId} AND coupon_id = #{couponId}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_coupon (user_id, coupon_id, status, create_time)
        VALUES (#{userId}, #{couponId}, #{status}, NOW())
    </insert>

    <update id="use">
        UPDATE user_coupon SET status = 1, order_id = #{orderId}, used_time = NOW() WHERE id = #{id} AND status = 0
    </update>

    <update id="updateStatus">
        UPDATE user_coupon SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateExpiredStatus">
        UPDATE user_coupon uc
        INNER JOIN coupon c ON uc.coupon_id = c.id
        SET uc.status = 2 WHERE uc.status = 0 AND c.end_time &lt; NOW()
    </update>
</mapper>
