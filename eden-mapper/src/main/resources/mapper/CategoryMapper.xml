<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eden.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="eden.pojo.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="icon" property="icon"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, parent_id, level, sort_order, icon, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM category
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM category
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM category
        WHERE parent_id = #{parentId} AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="selectFirstLevel" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM category
        WHERE parent_id = 0 AND status = 1
        ORDER BY sort_order ASC
    </select>

    <insert id="insert" parameterType="eden.pojo.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, parent_id, level, sort_order, icon, status, create_time, update_time)
        VALUES (
            #{name, jdbcType=VARCHAR},
            #{parentId, jdbcType=BIGINT},
            #{level, jdbcType=INTEGER},
            #{sortOrder, jdbcType=INTEGER},
            #{icon, jdbcType=VARCHAR},
            #{status, jdbcType=INTEGER},
            NOW(),
            NOW()
        )
    </insert>

    <update id="update">
        UPDATE category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="level != null">level = #{level},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="icon != null">icon = #{icon},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <update id="updateStatus">
        UPDATE category SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>
</mapper>
