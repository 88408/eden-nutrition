<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eden.mapper.NoticeMapper">

    <resultMap id="BaseResultMap" type="eden.pojo.Notice">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="target" property="target"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
        <result column="read_time" property="readTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, title, content, target, is_read, create_time, read_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM notice
        WHERE user_id = #{userId}
        ORDER BY create_time DESC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM notice
        WHERE user_id = #{userId}
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*)
        FROM notice
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markRead">
        UPDATE notice
        SET is_read = 1, read_time = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllRead">
        UPDATE notice
        SET is_read = 1, read_time = NOW()
        WHERE user_id = #{userId} AND is_read = 0
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice (user_id, type, title, content, target, is_read, create_time)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{target}, #{isRead}, NOW())
    </insert>
</mapper>
