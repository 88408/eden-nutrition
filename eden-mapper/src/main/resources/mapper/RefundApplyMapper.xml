<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eden.mapper.RefundApplyMapper">

    <resultMap id="BaseResultMap" type="eden.pojo.RefundApply">
        <id column="id" property="id"/>
        <result column="refund_no" property="refundNo"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="order_item_id" property="orderItemId"/>
        <result column="user_id" property="userId"/>
        <result column="refund_amount" property="refundAmount"/>
        <result column="original_order_status" property="originalOrderStatus"/>
        <result column="reason" property="reason"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="audit_remark" property="auditRemark"/>
        <result column="auditor_id" property="auditorId"/>
        <result column="audit_time" property="auditTime"/>
        <result column="refund_trade_no" property="refundTradeNo"/>
        <result column="simulated" property="simulated"/>
        <result column="refund_time" property="refundTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, refund_no, order_id, order_no, order_item_id, user_id, refund_amount, original_order_status, reason, images, status,
        audit_remark, auditor_id, audit_time, refund_trade_no, simulated, refund_time, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM refund_apply WHERE id = #{id}
    </select>

    <select id="selectByRefundNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM refund_apply WHERE refund_no = #{refundNo}
    </select>

    <select id="selectActiveByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM refund_apply
        WHERE order_no = #{orderNo}
          AND status IN (0, 1, 3)
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM refund_apply
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1) FROM refund_apply WHERE user_id = #{userId}
    </select>

    <select id="selectAdminPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM refund_apply
        <where>
            <if test="status != null">AND status = #{status}</if>
            <if test="keyword != null and keyword != ''">
                AND (refund_no LIKE CONCAT('%', #{keyword}, '%') OR order_no LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAdminPage" resultType="long">
        SELECT COUNT(1)
        FROM refund_apply
        <where>
            <if test="status != null">AND status = #{status}</if>
            <if test="keyword != null and keyword != ''">
                AND (refund_no LIKE CONCAT('%', #{keyword}, '%') OR order_no LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refund_apply (refund_no, order_id, order_no, order_item_id, user_id, refund_amount, original_order_status, reason, images, status, create_time, update_time)
        VALUES (#{refundNo}, #{orderId}, #{orderNo}, #{orderItemId}, #{userId}, #{refundAmount}, #{originalOrderStatus}, #{reason}, #{images}, COALESCE(#{status}, 0), NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE refund_apply
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="auditRemark != null">audit_remark = #{auditRemark},</if>
            <if test="auditorId != null">auditor_id = #{auditorId},</if>
            <if test="auditTime != null">audit_time = #{auditTime},</if>
            <if test="refundTradeNo != null">refund_trade_no = #{refundTradeNo},</if>
            <if test="simulated != null">simulated = #{simulated},</if>
            <if test="refundTime != null">refund_time = #{refundTime},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>
</mapper>
